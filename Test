-- Script to create a "No Cd Reb" button that, when pressed, spams the rebirth remote for 5 seconds,
-- then automatically makes the player quit the Roblox experience (kick themselves).

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Function to spam the rebirthRemote and then quit the experience after 5 seconds
local function AutoRebirthAndQuit()
    local startTime = tick()
    while tick() - startTime < 5 do
        ReplicatedStorage.rEvents.rebirthRemote:InvokeServer("rebirthRequest")
        task.wait(0.01)
    end
    -- Kick the local player from the experience
    Players.LocalPlayer:Kick("No Cd Reb is Bypass Lol")
end

-- UI creation
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "NoCdRebGui"
screenGui.Parent = playerGui

local button = Instance.new("TextButton")
button.Name = "NoCdRebButton"
button.Size = UDim2.new(0, 160, 0, 50)
button.Position = UDim2.new(0, 30, 0, 150)
button.BackgroundColor3 = Color3.fromRGB(255, 90, 90)
button.Text = "No Cd Reb"
button.Font = Enum.Font.SourceSansBold
button.TextSize = 24
button.Parent = screenGui

button.MouseButton1Click:Connect(function()
    button.Text = "Activated"
    button.BackgroundColor3 = Color3.fromRGB(100, 255, 100)
    button.Active = false
    task.spawn(AutoRebirthAndQuit)
end)
